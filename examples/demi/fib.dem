// fib.dem — Fibonacci numbers
fn fib(n: i32) -> i32 {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

pub fn main() -> i32 {
    let result = fib(10);
    return result;  // 55
}
